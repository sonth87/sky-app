---
"@sky-app/shell-electron": patch
"@sky-app/module-tts-studio": patch
---

Chuyển thanh công cụ của đoạn đang chọn trong Story (tách/nhân bản/âm lượng/xoá/sinh lại/chọn bản khác) từ popup nổi bám theo vị trí đoạn trên canvas sang thanh toolbar cố định phía trên timeline (giống voicebox) — trước đây đoạn ở track trên cùng đẩy popup lên toạ độ âm, bị vùng cuộn của timeline cắt mất hoàn toàn, không cách nào thấy được dù đã chọn đúng đoạn.
