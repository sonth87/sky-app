---
"@sky-app/tts-engine-ui": minor
"@sky-app/device-shell": patch
---

Tab "Hiệu ứng" trong cửa sổ Cấu hình TTS: slider chỉnh tham số hiệu ứng giờ có kiểu dáng riêng (không còn thanh trượt mặc định của trình duyệt), và ô chọn giọng để nghe thử đổi sang bộ chọn giọng đầy đủ (tìm kiếm, ảnh minh hoạ, nghe thử ngay trong danh sách) — giống hệt ô chọn giọng đọc trong TTS Studio.
