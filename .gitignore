node_modules/
dist/
dist-electron/
dist-lib/
.turbo/
*.log
.DS_Store
.env
.env.local
*.local

# Local dependency tarballs (built from external repos, e.g. device-layout).
# Not committed — regenerate via scripts/vendor-device-layout.sh (see .vendor/README.md).
.vendor/*.tgz

# License private keys — KHÔNG BAO GIỜ commit
*.private.pem
license-signing-key*

# apps/tts-service (Python) — venv/build tự tạo lại, không commit binary nặng
apps/tts-service/venv/
apps/tts-service/build/
apps/tts-service/dist/
__pycache__/
*.pyc
